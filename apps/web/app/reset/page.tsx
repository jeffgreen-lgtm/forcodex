import { ResetExperience } from "./ResetExperience";

export const metadata = {
  title: "Reset Password | CosmoScope",
  description: "Set a new CosmoScope password and return to your reading."
};

export default function ResetPage() {
  return <ResetExperience />;
}
