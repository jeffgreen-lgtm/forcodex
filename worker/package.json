{
  "name": "@cosmoscope/worker",
  "private": true,
  "type": "module",
  "scripts": {
    "deploy": "wrangler deploy",
    "dev": "wrangler dev",
    "smoke": "node scripts/smoke-test.mjs",
    "typecheck": "tsc --noEmit -p tsconfig.json"
  },
  "dependencies": {
    "@cosmoscope/api": "workspace:*",
    "@cosmoscope/core": "workspace:*",
    "@cosmoscope/db": "workspace:*",
    "astronomy-engine": "^2.1.19"
  },
  "devDependencies": {
    "typescript": "~6.0.3",
    "wrangler": "^4.23.0"
  }
}
