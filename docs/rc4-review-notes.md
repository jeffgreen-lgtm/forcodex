# RC4 review notes

Review the production-readiness pass in this order:

1. Home page metadata and keyboard navigation
2. `/app?mode=signup&source=landing` loading and recovery behavior
3. A deliberately unknown route to verify the branded 404
4. `/robots.txt`, `/sitemap.xml`, and `/manifest.webmanifest`
5. Mobile widths and reduced-motion settings

The approved RC2 visual treatment and RC3 conversion structure should remain unchanged.
